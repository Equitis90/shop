<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
</head>
<body>
<h1>Новый заказ на сайте</h1>
<h2>Заказ:</h2>
<table class="table" style="width: 100%">
  <thead>
  <tr>
    <th>Название</th>
    <th>Цена</th>
    <th>Количество</th>
    <th>Сумма</th>
  </tr>
  </thead>
  <tbody>
    <% @cart['items'].each do |id, item| %>
      <tr>
        <td>
          <b><%= item['title'] %></b>
        </td>
        <td>
          <b><%= number_to_currency(item['price'].to_f, :unit => "₴") %></b>
        </td>
        <td>
          <b><%= item['count'] %></b>
        </td>
        <td>
          <b><%= number_to_currency(item['sum'].to_f, :unit => "₴") %></b>
        </td>
      </tr>
    <% end %>
    <tr class="success">
      <td>&nbsp;</td>
      <td><b>Итого:</b></td>
      <td><b><%= @cart['count'] %></b></td>
      <td>
        <b><%= number_to_currency(@cart['sum'].to_f, :unit => "₴") %></b>
      </td>
      <td/>
    </tr>
  </tbody>
</table>
<br>
<h3>Номер телефона заказчика: <b><%= @phone %></b></h3>
</body>
</html>